# Firewall settings for production server
# Inbound rules for TCP port 80 and 443
# Rate-limiting enabled to prevent DoS attacks
# External access restricted to trusted IPs

[server]
host = 127.0.0.1
port = 8080

[database]
type = postgresql
host = localhost
port = 5432
user = admin
